#!/usr/bin/python3
from math import *
from sys import *


def factorize(number):
    max = isqrt(number) + 1
    for i in range(max):
        if (number % i == 0):
            return(i)
    return (-1)

if __name__=='__main__':
    f = open(argv[1], "r")
    for line in f:
        n = int(line)
        q = factorize(n)
        p = n // q
        print("{}={}*{}".format(n, p, q))
    f.close()
